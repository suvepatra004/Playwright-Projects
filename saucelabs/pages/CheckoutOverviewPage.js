import { CheckoutOverviewLocator } from "../locators/CheckoutOverviewLocator";

export class CheckoutPageOverview {
  constructor(page) {
    this.page = page;
  }

  async checkoutOverviewPageValidaton() {}
}
